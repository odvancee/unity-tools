## CreateFile

### Concept:

Create text files in the Project window context menu.

### Features

- Adding new formats is easy (copy-paste any method and adjust to your format)
- Files can be created with a predefined content (adjust the `content` argument in `Create` method calls)
- Supported File Types: `.md`, `.txt`, `.json`

### Usage:

- Menu: Assets > Create > File